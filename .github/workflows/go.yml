# Build, lint, race-test and vulnerability-scan the Go project.
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-go

name: Build Go Project

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

# CI only reads the repo; never grant the default write token to build jobs.
permissions:
  contents: read

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v7

    - name: Set up Go
      uses: actions/setup-go@v7
      with:
        go-version-file: go.mod

    # Mirror the pre-commit checklist from CODING_STYLE.md / CONTRIBUTING.md so
    # the quality gates are enforced, not just documented.
    - name: gofmt
      run: |
        unformatted="$(gofmt -l .)"
        if [ -n "$unformatted" ]; then
          echo "These files are not gofmt-clean:"
          echo "$unformatted"
          exit 1
        fi

    - name: go vet
      run: go vet ./...

    - name: Build
      run: go build -v ./...

    - name: Test (race)
      run: go test -race ./...

  govulncheck:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v7

    - name: Set up Go
      uses: actions/setup-go@v7
      with:
        go-version-file: go.mod

    # Scan dependencies and code paths for known Go vulnerabilities (CVEs).
    - name: govulncheck
      run: |
        go install golang.org/x/vuln/cmd/govulncheck@latest
        govulncheck ./...
